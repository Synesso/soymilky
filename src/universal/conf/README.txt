This directory is included into the distribution zipfile (as produced by sbt universal:packageBin)
so that when unzipped on the "target box" there is a conf directory present alongside bin/ and lib/

A valid account.config file should then be placed in this directory prior to application start.


